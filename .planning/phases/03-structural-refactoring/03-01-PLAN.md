---
phase: 03-structural-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - model/CBM.py
  - model/VCBM.py
  - model/CEM.py
  - model/__init__.py
  - model/mlp.py
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-02
  - STRUCT-03
user_setup: []
must_haves:
  truths:
    - "Common MLP class extracted to model/mlp.py for reuse"
    - "Module structure organized for better maintainability"
    - "Type hints added to __init__ parameters and forward() returns"
  artifacts:
    - path: "model/mlp.py"
      provides: "Shared MLP class"
      exports: ["MLP"]
    - path: "model/CBM.py"
      provides: "Main CBM implementation with type hints"
      imports: ["MLP from model.mlp"]
    - path: "model/VCBM.py"
      provides: "VCBM with type hints"
    - path: "model/CEM.py"
      provides: "CEM with type hints"
    - path: "model/__init__.py"
      provides: "Public exports unchanged for backward compatibility"
  key_links:
    - from: "model/VCBM.py"
      to: "model/CBM.py"
      via: "class VCBM(CBM)"
    - from: "model/CEM.py"
      to: "model/CBM.py"
      via: "class CEM(CBM)"
    - from: "model/CBM.py"
      to: "model/mlp.py"
      via: "from model.mlp import MLP"
---

<objective>
Extract common logic from CBM/VCBM/CEM models into shared modules, reorganize module structure, and add type hints to key public interfaces.

Purpose: Improve code architecture by extracting shared logic into base classes and utilities, reorganize for better maintainability, and add type safety to public APIs.

Output: Refactored model modules with extracted common logic, reorganized structure, and type-annotated interfaces.
</objective>

<execution_context>
@/home/jincheng1/.claude/get-shit-done/workflows/execute-plan.md
@/home/jincheng1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-structural-refactoring/03-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Current model structure
model/CBM.py     - Base CBM class (LightningModule), contains MLP class inline
model/VCBM.py    - Extends CBM, overrides forward and calc_loss
model/CEM.py     - Extends CBM, overrides __init__ and forward
model/backbone.py
model/__init__.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract MLP class to shared module</name>
  <files>model/mlp.py</files>
  <action>
    Create model/mlp.py with the MLP class extracted from CBM.py. Use type hints for __init__ parameters and forward method:
    - class MLP(nn.Module)
    - __init__(self, input_dim: int, hidden_dim: int, output_dim: int) -> None
    - forward(self, x: torch.Tensor) -> torch.Tensor
  </action>
  <verify>
    <automated>python -c "from model.mlp import MLP; m = MLP(10, 20, 5); print(type(m.forward(torch.randn(2, 10))))"</automated>
  </verify>
  <done>MLP class in model/mlp.py with type hints, importable</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update CBM to use shared MLP and add type hints</name>
  <files>model/CBM.py</files>
  <action>
    1. Update import to use: from model.mlp import MLP (instead of inline class)
    2. Add type hints to:
       - __init__ parameters (dm: LightningDataModule, base: str, use_pretrained: bool, etc.)
       - forward() returns: dict[str, torch.Tensor]
       - calc_loss() parameters and returns
       - training_step, validation_step, test_step methods
    3. Keep LightningModule interface signatures intact for checkpoint compatibility
  </action>
  <verify>
    <automated>python -c "from model import CBM; print('CBM imported successfully')"</automated>
  </verify>
  <done>CBM uses imported MLP, type hints added to public methods</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add type hints to VCBM and CEM</name>
  <files>model/VCBM.py, model/CEM.py</files>
  <action>
    Add type hints to VCBM and CEM classes:
    - VCBM.__init__ parameters (vib: float, res_dim: int, hsic_weight: float, hsic_kernel: str)
    - VCBM.forward() returns: dict[str, torch.Tensor]
    - VCBM.calc_loss() parameters and returns
    - CEM.__init__ parameters (embed_dim: int)
    - CEM.forward() returns: dict[str, torch.Tensor]
    Ensure imports are correct (may need updating from model import CBM to from model.CBM import CBM)
  </action>
  <verify>
    <automated>python -c "from model import VCBM, CEM; print('VCBM and CEM imported successfully')"</automated>
  </verify>
  <done>VCBM and CEM have type hints on public methods</done>
</task>

</tasks>

<verification>
- All model classes importable without errors
- MLP can be instantiated and called
- CBM, VCBM, CEM inheritance chain works
- LightningModule checkpoint loading still works (backward compatible)
- No new runtime errors in model imports
</verification>

<success_criteria>
1. Common MLP class extracted to model/mlp.py (STRUCT-01)
2. Type hints added to CBM, VCBM, CEM public methods (STRUCT-03)
3. Module exports unchanged in model/__init__.py (backward compatible)
4. All imports work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-structural-refactoring/03-01-SUMMARY.md`
</output>
