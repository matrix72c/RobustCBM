---
phase: 04-quality-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - tests/smoke/test_model.py
  - tests/smoke/test_config.py
  - tests/conftest.py
  - main.py
autonomous: false
requirements:
  - QUAL-04
  - VALID-01
  - VALID-02
  - VALID-03
  - VALID-04

must_haves:
  truths:
    - "Test coverage for core functionality: config loading, model instantiation, forward pass"
    - "Training pipeline executes successfully (quick smoke test with 1 epoch)"
    - "Evaluation pipeline executes successfully"
    - "Model checkpoint loading works (backward compatibility preserved)"
  artifacts:
    - path: "tests/smoke/test_model.py"
      provides: "Model instantiation and forward pass tests"
      min_lines: 50
    - path: "tests/smoke/test_config.py"
      provides: "Config loading tests"
      min_lines: 20
  key_links:
    - from: "tests/smoke/test_model.py"
      to: "model/CBM.py"
      via: "from model import CBM"
      pattern: "from model import"
    - from: "main.py"
      to: "checkpoints"
      via: "load_from_checkpoint"
      pattern: "load_from_checkpoint"
---

<objective>
Add comprehensive test coverage and verify all functionality preserved.

Purpose: Ensure core functionality is tested and all pipelines work correctly after refactoring.

Output: Updated tests, validated training/evaluation pipelines, verified checkpoint loading.
</objective>

<context>
@/home/jincheng1/RobustCBM/tests/conftest.py
@/home/jincheng1/RobustCBM/tests/smoke/test_model.py
@/home/jincheng1/RobustCBM/tests/smoke/test_config.py
@/home/jincheng1/RobustCBM/.planning/phases/04-quality-validation/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test coverage for core functionality</name>
  <files>tests/smoke/test_model.py, tests/conftest.py</files>
  <action>
    Expand test coverage for core functionality using pytest:
    - Add test_config_loads_successfully: loads config.yaml without error
    - Add test_config_required_keys: validates model, dataset keys present
    - Add test_model_instantiation: creates CBM model instance (mock DataModule if needed)
    - Add test_model_forward_pass: runs forward pass with dummy input
    - Add test_model_calc_loss: verifies loss computation works
    - Add test_checkpoint_loading: verifies load_from_checkpoint works

    Use pytest fixtures from conftest.py for model/dataset setup.
    Focus on quick smoke tests that verify pipeline works.
  </action>
  <verify>
    <automated>cd /home/jincheng1/RobustCBM && python -m pytest tests/smoke/ -v --tb=short 2>&1 | head -50</automated>
  </verify>
  <done>Tests for config loading, model instantiation, forward pass, loss computation exist and pass</done>
</task>

<task type="auto">
  <name>Task 2: Verify training pipeline works</name>
  <files>main.py</files>
  <action>
    Verify training pipeline executes successfully after refactoring:
    - Run training with minimal config (1 epoch, small batch size if possible)
    - Verify trainer initializes without errors
    - Verify training_step executes without errors

    If config.yaml exists, use it; otherwise create minimal test config.
    This is a smoke test - not full training, just verify pipeline works.
  </action>
  <verify>
    <automated>cd /home/jincheng1/RobustCBM && python -c "from main import train; print('train function loads OK')"</automated>
  </verify>
  <done>Training pipeline code loads and initializes without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Training and evaluation pipelines need to be tested</what-built>
  <how-to-verify>
    Run: python main.py --config config.yaml --task_id 0

    OR if no config exists, verify:
    1. python -c "from main import train, evaluate; print('OK')" - imports work
    2. Check if any checkpoints exist to test loading

    Expected: Training/evaluation functions execute without runtime errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify checkpoint loading (backward compatibility)</name>
  <files>main.py</files>
  <action>
    Verify model checkpoint loading works:
    - Check if existing checkpoints exist in checkpoints/ directory
    - If checkpoint exists, verify load_from_checkpoint works with refactored model
    - Ensure LightningModule interface preserved (training_step, validation_step, forward signatures unchanged)

    Per user decision: Load existing checkpoint with refactored model to verify compatibility.
    Use strict=False if needed for backward compatibility with layer name changes.
  </action>
  <verify>
    <parameter name="command">ls /home/jincheng1/RobustCBM/checkpoints/*.ckpt 2>/dev/null | head -3</parameter>
    <description>Check for existing checkpoints</description>
  </verify>
  <done>Checkpoint loading verified (or verified no existing checkpoints to break)</done>
</task>

</tasks>

<verification>
- [ ] Test coverage for core functionality exists (config loading, model instantiation, forward pass)
- [ ] Training pipeline executes without errors (smoke test)
- [ ] Evaluation pipeline executes without errors
- [ ] Model checkpoint loading works (backward compatibility preserved)
- [ ] Results before and after refactoring can be compared (if checkpoints exist)
</verification>

<success_criteria>
Comprehensive test coverage for core functionality exists.
Training pipeline executes successfully after refactoring.
Evaluation pipeline executes successfully after refactoring.
Model checkpoint loading works (backward compatibility preserved).
Results before and after refactoring can be compared.
</success_criteria>

<output>
After completion, create .planning/phases/04-quality-validation/04-02-SUMMARY.md
</output>
