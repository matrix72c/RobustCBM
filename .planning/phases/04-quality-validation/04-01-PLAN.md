---
phase: 04-quality-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - model/CBM.py
  - model/VCBM.py
  - model/CEM.py
  - main.py
  - dataset/__init__.py
  - utils.py
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03

must_haves:
  truths:
    - "Error handling added to config loading, dataset init, model __init__, forward pass"
    - "Docstrings added to all public APIs (LightningModule, DataModule, key functions)"
    - "Logging replaces print statements in model init, forward, training/validation steps"
  artifacts:
    - path: "model/CBM.py"
      provides: "CBM model with error handling, docstrings, logging"
      min_lines: 250
    - path: "main.py"
      provides: "Training script with error handling, logging"
      min_lines: 120
    - path: "dataset/__init__.py"
      provides: "Dataset loading with error handling"
      exports: ["CUB", "AwA", "celeba"]
  key_links:
    - from: "main.py"
      to: "dataset/__init__.py"
      via: "getattr(dataset, cfg['dataset'])"
      pattern: "getattr\\(dataset,"
    - from: "main.py"
      to: "model"
      via: "getattr(pl_model, cfg['model'])"
      pattern: "getattr\\(pl_model,"
---

<objective>
Add error handling, docstrings, and logging to core modules.

Purpose: Improve robustness with proper error handling, maintainability with docstrings, and observability with logging.

Output: Updated model files, main.py, and dataset modules with error handling, docstrings, and logging.
</objective>

<context>
@/home/jincheng1/RobustCBM/model/CBM.py
@/home/jincheng1/RobustCBM/main.py
@/home/jincheng1/RobustCBM/dataset/__init__.py
@/home/jincheng1/RobustCBM/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error handling to config loading and dataset init</name>
  <files>main.py, dataset/__init__.py</files>
  <action>
    Add error handling to config loading (main.py load_checkpoint and train functions):
    - Wrap config loading in try/except with ValueError for missing/invalid config keys
    - Add validation for required model/dataset names in config
    - Raise FileNotFoundError if config file not found with detailed message
    - Add error handling in dataset initialization: validate required DataModule attributes exist

    Add import for logging module at top of main.py:
    - import logging
    - logger = logging.getLogger(__name__)

    Use standard Python exceptions (ValueError, RuntimeError, FileNotFoundError) per user decision.
  </action>
  <verify>
    <automated>python -c "import main; print('main.py imports OK')"</automated>
  </verify>
  <done>Config loading raises informative errors on missing/invalid config; dataset init validates required attributes</done>
</task>

<task type="auto">
  <name>Task 2: Add NumPy-style docstrings to public APIs</name>
  <files>model/CBM.py, model/VCBM.py, model/CEM.py</files>
  <action>
    Add NumPy-style docstrings to all public API methods in CBM, VCBM, CEM classes:
    - __init__: purpose, args (dm, base, use_pretrained, etc.), returns None
    - configure_optimizers: purpose, returns optimizer config dict
    - forward: purpose, args (x, concept_pred optional), returns Dict[str, Tensor]
    - calc_loss: purpose, args (gt, pred), returns losses dict
    - training_step: purpose, args (batch, batch_idx), returns Tensor
    - validation_step: purpose, args (batch, batch_idx), returns None
    - test_step: purpose, args (batch, batch_idx), returns None

    Follow user decision: minimal docstrings (purpose + args only), NumPy style, public APIs only.
  </action>
  <verify>
    <automated>python -c "from model import CBM, VCBM, CEM; print('Models import OK with docstrings')"</automated>
  </verify>
  <done>All public LightningModule methods have NumPy-style docstrings with purpose and args</done>
</task>

<task type="auto">
  <name>Task 3: Add logging to key functions</name>
  <files>model/CBM.py, model/VCBM.py, model/CEM.py, main.py</files>
  <action>
    Add Python logging to key functions per user decision:
    - Add import logging and logger = logging.getLogger(__name__) at module level
    - Model __init__: log INFO message with model config summary (num_classes, num_concepts, backbone)
    - Forward pass: log INFO at key branches (cbm_mode handling)
    - Training step: log INFO at epoch start (can use Lightning's logging)
    - Validation step: log INFO with metrics
    - In main.py: replace print statements with logging.INFO for run_id, run_name

    Use standard levels: INFO for progress, WARNING for recoverable, ERROR for failures.
    Integrate with Lightning's logger where appropriate.
  </action>
  <verify>
    <automated>python -c "from model import CBM; import logging; print('Logging setup OK')"</automated>
  </verify>
  <done>Logging replaces print statements; model init, forward, training/validation steps have logging</done>
</task>

</tasks>

<verification>
- [ ] Error handling added: ValueError for config issues, RuntimeError for model issues, FileNotFoundError for data
- [ ] Docstrings added: All public APIs have NumPy-style (purpose + args)
- [ ] Logging added: Python logging module in model __init__, forward, training/validation steps
- [ ] No existing functionality broken (smoke tests pass)
</verification>

<success_criteria>
Error handling added to config loading, dataset init, model __init__, forward pass with appropriate exceptions.
Docstrings added to all public APIs explaining purpose, args, returns.
Logging replaces print statements for debugging and monitoring in key functions.
</success_criteria>

<output>
After completion, create .planning/phases/04-quality-validation/04-01-SUMMARY.md
</output>
